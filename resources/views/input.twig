<select name="{{ name }}" class="{{ class }} form-control selectpicker">

    {% for option in options %}

        <option value="{{ option.value }}" {{ option.selected ? 'selected' }}>{{ option.title }}</option>

    {% endfor %}

</select>